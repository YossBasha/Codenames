### Git and Release Constraints
- **Never push to remote repositories** (`git push`) or trigger releases/tags without explicit, prior permission from the user.
- Always ask the user for confirmation before executing commands that modify remote state or trigger CI/CD workflows.
